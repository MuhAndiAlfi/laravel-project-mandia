1. import Database
2. `composer install/update`
3. jika tidak ada tablenya bisa jalankan di terminal `php artisan migrate`
   - untuk dataseednya bisa jalankan `php artisan db:seed`
4. lalu `php -S localhost:8000 -t public` untuk menjalankan servernya